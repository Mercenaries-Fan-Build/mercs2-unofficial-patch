# Mercenaries 2 unofficial patch — per-fix switches. Set a key to 0 to disable that fix without
# rebuilding. All ON by default; results are logged to unofficial_patch.log next to this file.

# Profile changes to cash/fuel-capacity/character/costume are not autosaved when nothing else changed.
BUG_004 = 1

# Three PDA blip binders dereference NULL on a stale or out-of-range blip id. Crash, not a glitch.
BUG_007 = 1

# Gui.ShowLoadingHints(false) never turns loading hints off.
BUG_008 = 1

# Pg.UnloadLayer dereferences NULL on a co-op CLIENT when no game state is current.
BUG_009 = 1

# BUG-005 and BUG-011 have no key here because they have no fix here — see native/src/fixpack.h.
