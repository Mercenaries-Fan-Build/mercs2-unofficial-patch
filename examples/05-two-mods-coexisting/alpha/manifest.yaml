format: 1
shipment:
  name: alpha-mod
  version: 1.0.0
  target: retail
  description: Half of the coexistence demo — patches the same script as the other half.

contributions:
  - kind: patch_lua
    target: wifpmcinterior
    append: src/append.lua
