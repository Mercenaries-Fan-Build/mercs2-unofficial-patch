# qm output. Built artifacts are reproducible from the manifest and src/, and a committed WAD is a
# 2 MB binary that will conflict on every merge.
build/

.DS_Store
Thumbs.db

# Built native artifact. Reproduced by `make -C sdk build && make -C native`; a committed binary is
# the one a consumer accidentally ships instead of the one they just built.
src/unofficial_patch.asi
